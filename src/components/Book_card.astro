---
import "../global.css";
import Button from "./Button.astro";
---

<div class="bookcard">
  <img class="hjerte" src="redheart.svg" alt="hjerte" />
  <h3>HAR DU BRUG FOR AT TALE MED NOGEN OM DIT BARNS TRIVSEL?</h3>
  <img class="calendar" src="calendar.svg" alt="kalender" />
  <p>Er du i tvivl om eller bekymret for dit barns trivsel, er det vigtigt at række ud efter hjælp.</p>
  <div class="button">
    <Button variant="secondary">Book tid</Button>
  </div>
</div>

<style>
  .bookcard {
    background-color: var(--secondary);
    color: #fff;
    padding: 2rem 2rem;
    border-radius: 20px;
    position: relative;
    margin: 2rem 1rem;
    min-width: 325px;
    max-width: 450px;
    max-height: 550px;
  }

  .hjerte {
    margin: 0 auto;
  }
  .calendar {
    position: absolute;
    right: 1rem;
    top: 9rem;
    transform: scale(75%);
  }
  h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    max-width: 92%;
    color: #fff;
  }

  p {
    max-width: 80%;
    line-height: 1.8rem;
    margin-bottom: 2rem;
    color: #fff;
  }

  .button {
    text-align: center;
  }

  @media only screen and (max-width: 900px) {
    .calendar {
      display: none;
    }
  }
</style>
